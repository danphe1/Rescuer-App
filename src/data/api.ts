import type{QueueRecord}from'../domain/types';
const endpoint=import.meta.env.VITE_RESCUE_API_URL as string|undefined;
export async function upload(record:QueueRecord){if(!endpoint)throw new Error('Server integration is not configured');const token=sessionStorage.getItem('rescue-device-token');if(!token)throw new Error('Approved device session required');const body=record.kind==='GPS'?{action:'location',points:[record.payload]}:record.kind==='SOS'?{action:'sos',...record.payload}:record.kind==='MEDIA'?{action:'mission_photo',...record.payload}:record.kind==='CHECK_IN'?{action:'note',...record.payload}:record.payload;const response=await fetch(endpoint,{method:'POST',headers:{'content-type':'application/json','x-device-token':token},body:JSON.stringify(body)});if(!response.ok)throw new Error(`Server rejected action (${response.status})`);return response.json()}
