import{useEffect,useRef,useState}from'react';import{policy}from'../domain/policy';
export function SosHoldButton({onConfirm,disabled=false}:{onConfirm:()=>void;disabled?:boolean}){const[start,setStart]=useState(0);const[progress,setProgress]=useState(0);const frame=useRef(0);function stop(){cancelAnimationFrame(frame.current);setStart(0);setProgress(0)}useEffect(()=>()=>cancelAnimationFrame(frame.current),[]);function begin(){if(disabled)return;const began=performance.now();setStart(began);const tick=(now:number)=>{const p=Math.min(1,(now-began)/policy.sos.holdMs);setProgress(p);if(p>=1){setStart(0);onConfirm()}else frame.current=requestAnimationFrame(tick)};frame.current=requestAnimationFrame(tick)}return <button className="sos-hold" disabled={disabled} onPointerDown={begin} onPointerUp={stop} onPointerCancel={stop} onPointerLeave={stop} style={{'--hold':`${progress*100}%`}as React.CSSProperties}><span>{start?'HOLD 3 SEC…':'SOS'}</span><small>{start?`${Math.ceil(3-progress*3)} seconds — keep holding`:'Press and hold for 3 seconds'}</small></button>}
